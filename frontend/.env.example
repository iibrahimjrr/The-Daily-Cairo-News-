# Backend API URL — must match your Laravel server address
VITE_API_URL=http://localhost:8000/api/v1

# App name (shown in browser tab)
VITE_APP_NAME="The Daily Cairo"

# Pusher (optional — only needed for real-time features)
# VITE_PUSHER_APP_KEY=your_pusher_key
# VITE_PUSHER_APP_CLUSTER=mt1
